# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode (we do not use an Xcode project, but tooling may still litter)
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/

# macOS
.DS_Store
